package org.home.ds.tree;

public class InOrderSuccesor {
    /*public TreeNodeInt inOrderSuccessor(TreeNodeInt root, int key){
        if(root == null){
            return null;
        }else{
            if(root.key == key){
                return root;
            }
        }
    }*/
}
